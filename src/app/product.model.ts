export class Product {
  constructor (public name: string, public gender: string, public clothing: string, public price: number, public size: string, public color: string, public quantity: number, public description: string, public imageUrl: string, public id: number) {}
}
